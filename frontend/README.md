# Clinic AI — frontend

Vite + React. Local dev: `pnpm run dev` (port **1201**).

- **Env**: copy `.env.example` → `.env` and set `NEXT_PUBLIC_API_URL` (required in production) and `NEXT_PUBLIC_SITE_URL` (canonical links, Stripe return URLs, chat/embed snippets).
- **E2E / launch**: [TESTING.md](./TESTING.md); full-stack checklist: [../LAUNCH_CHECKLIST.md](../LAUNCH_CHECKLIST.md).
